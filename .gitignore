# Dart and Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Android build output. The android/ project itself is committed so the app
# clones and runs without a `flutter create .` step first.
/android/.gradle/
/android/app/build/
/android/local.properties
/android/.idea/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db
